<div class="book-image">
  <% if (book.image_url) { %>
  <img src="<%= book.image_url %>" alt="<%= book.title %>" />
  <% } else { %>
  <img src="/default-book.png" alt="No image available" />
  <% } %>
</div>
